{
  "name": "flash-pump-engine",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx packages/shared/src/db/migrate.ts",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.5",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
